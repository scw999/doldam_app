node_modules/
apps/api/log_img/
.wrangler/
.dev.vars
.env
.env.local
*.log
.DS_Store

# Expo
.expo/
.expo-shared/
dist/
web-build/

# IDE
.vscode/
.idea/

# Build
*.tsbuildinfo

# Firebase
# google-services.json / GoogleService-Info.plist 는 클라이언트 설정 파일로
# 비밀이 아니며(APK/IPA에 노출되는 값), 커밋 필요 — EAS Build가 git 추적 파일만 업로드함
# 단 서비스 계정 키(Admin SDK)는 절대 커밋 금지
*-firebase-adminsdk-*.json
fcm-service-account.json
serviceAccount*.json
